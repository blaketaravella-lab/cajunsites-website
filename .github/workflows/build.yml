name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install
      - name: Validate Worker JavaScript
        run: |
          set -e
          for file in worker/*.js; do
            echo "Checking $file"
            node --check "$file"
          done
      - name: Build Astro site
        run: npm run build
